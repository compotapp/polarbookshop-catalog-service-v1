apiVersion: apps/v1
kind: Deployment
metadata:
  name: pbs-catalog-service
spec:
  template:
    spec:
      containers:
        - name: pbs-catalog-service
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: staging